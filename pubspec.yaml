name: dmdata_telegram_json
description: Project DM-D.S.S Json Telegram Parser for Dart and Flutter projects
version: 0.0.13
repository: https://github.com/YumNumm/DmdataTelegramJson

environment:
  sdk: ">=2.18.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.7.0

dev_dependencies:
  build_runner: ^2.3.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.2.0
  json_serializable: ^6.5.3
  pedantic_mono: ^1.20.0
  test: ^1.21.4

flutter: null
